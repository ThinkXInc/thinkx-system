2026-09-17 データ完全移行・編集UI拡張・staging 開通（生ログ）

引用（太字）が発案者の発言で、原文のまま。続く地の文が Claude の応答（実際に返した内容。
冗長な部分のみ圧縮）。時系列。確定事項は thinkx-system/docs/PODCAST_TRACK.md（D-52）と
docs/GUIDELINES.md 項26〜29 が正。

> **このセッションはpodcastのmonorepo化だっけ？久々に開いたので経緯を簡単いまとめて**

トラックの経緯（取り込み・Flask化・データ移行・現在地=ローカル確認のOK待ち）を要約して提示。

---

> **http://127.0.0.1:8011/ の挙動は問題ないので 元のフォルダで編集した内容を取り込んでくれ　若干仕組みが変更されていたりするので壊さないように慎重に　元のフォルダは変更しないように（完全に復元できるように）**

棚卸し（読み取り専用）→ 相違ゼロ・取り込み対象は旧側の新規109ファイルのみと確認。
109件をコピーで取り込み、既存464件はハードリンク共有を解消して独立コピー化
（以後 monorepo 側の編集が旧フォルダに及ばない）。旧フォルダは573ファイルの
パス・サイズ・mtime 照合で無変更を証明。編集データ9ファイルをコミット。

---

> **念のため旧フォルダの編集データが完全に移行されたか再度チェックせよ**  
> **それがOKなら今後はmonorepo内のデータを編集する**  
> **Sources/podcast/CLAUDE.mdには以下を書いておいた**  
> **\*\*\* 2026/09/17にmonorepoのthinkx-system/podcastに完全に移行した．**  
> **このフォルダはバックアップとしてしばらく残す．以降もしこのフォルダのサーバーを起動して編集を試みるなどしたら誤ったフォルダで作業をしていないかアラートを出せ \*\*\***

全573ファイル（10.8GB）を SHA-256 で読み比べ、全一致（うち編集データ43件すべて存在・一致）。
旧サーバー（8010）は停止済みを確認。アラート義務を memory に保存し、移行完了を
PODCAST_TRACK に記録。

---

> **（切り抜き）とあるものは手をつけていないはず　それらの項目は音源一覧ページから消してくれ　切り抜きはメニューを分離せずその同じタイトルの最終編集版を元に同じメニュー内に生成することにするが詳しくは別途指示する**

一覧から（切り抜き）3件を非表示（データとIDページは残す）。新方式は指示待ちで未着手。
GUIDELINES 項26 に原文記録。

---

> **次にやることはこうだ**  
> **この編集で書き出す（m4a）　 ノイズ除去　の右に「本番使用」のチェックボックスをつけてくれ**  
> **このチェックがついたものは**  
> **.seg {**  
> **    border: 1px solid #ccc4;**  
> **    border-radius: 12px;**  
> **    padding: 16px 19px 21px;**  
> **    margin-bottom: 35px;**  
> **    background-color: #fff9e4;**  
>
> **のように背景を黄色にする**

「本番使用」チェックボックスを実装。状態は segments.json の production フラグとして保存
（履歴・ジャーナル・同期キューに乗る編集データ扱い）。ライトは指定色 #fff9e4、
ダークは同系の暗い黄。

---

> **そして「本番使用」のチェックボックスがONになったIDのコンテンツは音源一覧で背景を同じ黄色にし，編集済み->編集完了 に変える**  
> **ついでに「処理済み」は紛らわしいので「未編集」にし「未処理」と色を似せる（グレーが少し薄い濃い）**  
> **「編集済み」はチェックボックスがONでないなら「編集中」にする**

一覧のステータス体系を変更: 未処理 / 未編集（薄いグレー）/ 編集中（本番使用が付くまで）/
編集完了（本番使用あり・行背景黄）。4状態を実測確認。

---

> **.st-done {**  
> **    background: #804e7f;**

編集完了ラベルを紫 #804e7f に変更。

---

> **#f7effc**  
>
> **:root[data-theme="light"] ul.ids li.production {**  
> **    background: #f7effc;**  
>
> **編集完了チェックボックスの右にさらに「配信済み」チェックボックスをつけてくれ　背景は黄色でなくこの色に**  
> **ラベルは編集完了から「配信済み」に変える　紫のままでいい**

「配信済み」チェックボックスを追加（delivered フラグ・set_seg_flag に一般化）。
配信済みは編集完了より優先し、背景は薄紫・ラベルは紫のまま「配信済み」。

---

> **li.delivered {**  
> **    background: #fcefef;**  
>
> **やや調整した**

配信済みの背景色を #fcefef に調整（seg 側・一覧側とも）。

---

> **ここまででステージングにアップしよう**

手順書 2→3 で staging デプロイ（thinkx 等 4 サイト 200）。/src/podcast symlink の欠落を
発見して clone_monorepo / setup_podcast に追加。web1-stg で setup_podcast.sh 実行
（ffmpeg・venv・uwsgi 常駐・flusher timer → podcast 8010 = 200）。LB は direct.conf が
生きて 401（認証ファイル待ち）。LB 用の htpasswd コマンドを手渡し、データ搬入の ID 指定を確認。

---

> **その設定の意味をちゃんと説明しろ**  
> **データはローカルを完全にコピーする　この同期のルールが必要になるだろう　gitのcommitと連動していればいいだろう**

Basic 認証の仕組み（IP 直 URL は全ネットから到達可・編集UI自体に鍵がない・htpasswd が唯一の
鍵・私が代行しない理由）を説明。同期ルールを改定: 選択公開をやめ、デプロイ（= commit された
ものを出す操作 = git commit との連動点）のたびにローカル data/ 全体を差分同期。削除はしない。
edit/ は git が正。D-52 に改定注記・GUIDELINES 項27。全量同期を開始。

---

> **これだがgit lfs を使ってすべてのデータをgitで管理すると一貫性やシンプルさ，確実にならないか？この方法の利点はあるのか？**

比較を提示: LFS の一貫性は本物だが、(1) 全世代永久保存で容量・課金が単調増加
(2) 4台の checkout 全部に全量転送 (3) LFS 障害がサイトデプロイを止める
(4) サーバーの自動 commit が重くなる。失って困る層（編集判断）は既に git で一貫しており、
現方式の維持を推奨（弱点=機構2本・削除非伝播・サイズ比較も正直に提示）。

---

> **これはpodcastのサイトへのパスワード？stagingサイト全体？thinkxのstagingサイトは共通でパスワードを設定していたはずだがこれと重複したり干渉しない？**

完全に別ファイル（.htpasswd_podcast vs .htpasswd_staging）で干渉しない。分けた理由は
作業者に渡す鍵と staging 全体を覗ける鍵を同じにしないため。共通化も1行で可能と提示。

---

> **データ同期の方針は合意する**  
> **git lfsは却下だ**  
>
> **ローカルを消した場合どうなるか知りたい**  
> **stagingのディスク容量を考慮してデータを削除する可能性はある**

ローカル削除は伝播しない（サーバーに残る・編集判断は git に永続・音源の正本は失う）。
逆に staging だけ消しても次のデプロイで復活する、という規則の穴を明示。ここで
**staging のディスクが 20GB・残り 344MB と判明**し、進行中の全量同期を緊急停止・
一時ファイルを掃除。選択肢 a（ディスク拡張）/ b（`_` 始まりの退役規則）/ c（同期範囲縮小）を提示。

---

> **experimentsはただのノイズキャンセルの実験だから同期は必要ない　これがないとどうなる**

無くても何も起きない（パイプラインは読まない・UIも参照しない）。`_` 始まり除外規則を発効:
experiments → _experiments に改名、同期スクリプトに除外を実装、staging の複製 200MB を削除。

---

> **b. 退役(アーカイブ)規則を足す — ローカルで ID フォルダ名を _ 始まりに変えたものは「一覧にも出ない・同期もされない」とし、その上でサーバーから消す**  
>
> **a. ディスクを拡張する(推奨) — EBS を 20GB→50GB 程度へ。terraform の変更1行 + apply(承認制)。費用は +30GBで月2〜3百円程度。**  
>
> **この２つをやろう**  
> **continue**

a: terraform（web_disk_gb を staging も 50 に）→ plan で「in-place 1 change のみ」を確認して
apply → 機内 growpart/resize2fs → 49GB・空き31GB。手順を runbooks/disk-expand.md に手順書化。
b: 規則を PODCAST_TRACK・GUIDELINES 項28 に記録。全量同期を再開し完了（配布8・一致6・失敗0）。

---

> **user pas が通らない**

nginx のエラーログに podcast / thinkx / kobito での試行が残っており、登録済みユーザー名は
editor と判明（ファイル・ハッシュは正常）。ユーザー名の変更手順も提示。

---

> **認証は通ったがサイトが一向に出てこない　Failed to load resource: the server responded with a status of 504 (Gateway Time-out)**  
> **favicon.ico:1  GET http://52.68.142.190/favicon.ico 404 (Not Found)**

SG のバックエンド許可が 8000〜8009 で podcast の 8010 だけ範囲外（LB からの接続タイムアウトを
実測）。8010 まで拡大して apply → LB から 200。favicon 404 は設計通り。

---

> **8010などすぐ超えるのでは？また問題が起きないようにもう少し広げたら？**

8000〜8099 の100枠に拡大（LB SG からのみの内部許可なのでリスク増なし）。apply 済み。

---

> **http://52.68.142.190/podcast/id?id=%E3%83%A9%E3%83%B3%E3%83%81%E5%A4%A7%E6%B2%BC%E3%81%95%E3%82%93%EF%BC%A0%E3%83%9F%E3%83%83%E3%83%89%E3%82%BF%E3%82%A6%E3%83%B33-5**  
>
> **これがなぜか２つあるし編集データが表示されない**  
> **他の編集はしたが配信済みでないコンテンツもどれも２つずつ出てきていたりする　何かおかしいからチェックしろ**

原因を特定: ファイル名の正規化形の食い違い（mac=NFD 保存、git=NFC 変換、tar=NFD のまま）で、
Linux 上に「git 経由の edit/（NFC）」と「tar 経由の音源（NFD）」の同名別フォルダが分裂。
濁点を含む3フォルダが該当。サーバー側で NFD→NFC へ統合。

---

> **> 恒久対策として、同期スクリプトの展開後にサーバー側で NFC 正規化を走らせます(送信側=Mac は NFD のままでよく、サーバーが常に NFC に寄せる)。**  
>
> **これは本質的な解決法なの？冗長ではない？**

指摘を認め対症療法を撤回。根本原因は「同じツリーを2経路で運び、経路ごとに名前の形が違う」
こと。git の NFC は変えられないので、源流（ローカル data/）を NFC に正準化し、送信前に
1箇所でチェック+補正するのが本質、と提示。

---

> **NFCに揃えるのはやった方がいいだろう**  
> **同期する時にチェックしてアラートまたは補正が必要だろう**  
> **ID との整合性をよく確認すべきだろう**  
>
> **--**  
>（以下、NFD/NFC とは何か・今回の事故の機構についての Claude 説明の引用貼付）  
> **--**  
> **この説明で意味がわかった**

実装: scripts/normalize_data_names.py 新設（--check/--fix。mac の APFS は NFC/NFD を同一視して
照合するため、衝突判定は親一覧との厳密一致・リネームは一時名経由の2段が必要と実測）。
ローカル data/ を全 NFC 化（579ファイル・git status 不変）。同期スクリプトが送信前に毎回
--fix を通す。サーバーの NFD 残骸17件を掃除。整合性の実測: サーバー非NFC名 0・一覧11件
重複なし・編集データ表示・同期 配布0/一致14。findings と GUIDELINES 項29 に記録。

---

> **OK**  
> **この件は一旦完了 記録してセッションを閉じる claude generalの通り**

本議事録4本（8/24・8/25・8/28・9/1・9/17）を作成して記録し、セッションを閉じた。

## 到達点

podcast は monorepo に完全移行し（データは全ハッシュ照合済み・旧フォルダは凍結バックアップ）、
編集サイトは他サイト共通構造の Flask + uWSGI として staging で常時稼働。
http://{staging LBのEIP}/podcast/ に Basic 認証（editor）で到達でき、全データ同期・
編集の自動 git 記録・退役規則・NFC 正準化まで運用ルールが揃った。
残りは Phase 4（本番常設。production ブランチへのデータ push と release 運用の整合を
冒頭で設計）と Phase 5（fabula）。切り抜きの新方式は別途指示待ち。
