# docs/DECISIONS.md
#
# NNTMで合意済みの、現在有効な設計・リリース判断を記録する。

| ID | 決定 | 根拠・影響 | オーナー指示原文 |
|---|---|---|---|
| D-001 | ユーザーの編集指示は音源IDごとに `data/<ID>/編集メモ.md` の「編集指示ログ」に記録する（2026-07-26） | 指示の経緯を後から追跡できる。IDごとの決定は編集メモ、システム全体の決定は本ファイル、と役割を分ける | 「私がこのように指示したものを、その音源IDごとに記録をしてほしい。編集メモ.mdがそれにあたるか」 |
| D-002 | `data/<ID>/` 直下には元入力（音源・Notta PDF/txt・要約）とパイプラインが参照する生成物のみを置き、バックアップは `backup/`、suggestions 等の中間物は `中間生成物/` に分ける（2026-07-26） | ID フォルダの見通しを保つ。スクリプトが参照するファイルは直下から動かさない（動かすとパイプラインが壊れる） | 「data/{ID}中が散らかっているので整理してください。フォルダに役割ごとに分けてください」「その時に元に入っていたものはそのままID直下に置いておいてください。例えば、PDF、要約、音源など」 |
| D-003 | システム全体に関する決定事項は本ファイル `docs/DECISIONS.md` に追記していく（2026-07-26） | 個々の音源の編集判断（編集メモ.md）と全体設計の判断を分離する | 「docs/DECISIONS.md というファイルを作り、ここに個々の音源の編集ではなく、この編集システム全体に関する決定事項を追記していってください。基本的には私が決定判断を下したものが列挙されていきます」 |
| D-004 | カット推奨には音源をまたいでユニークな番号 `C<音源No>-<連番>`（例 C1-2）を振り、見出しの前に表示する。台帳は `data/sources.json`（ID→音源No）と `data/<ID>/cut_decisions.json`（status: pending/cut/keep）。付番は `scripts/assign_cut_ids.py <ID>`（2026-07-26） | 「C1-2はカット、C1-3はカットしない」と番号で指示でき、一度判断したものは再チェック不要になる | 「一度これはカットすべき、またはすべきでないと判断したものについては、もう一度チェックしなくていいようにしたい。そのために、カット推奨の箇所にはナンバーを振ってください。そのナンバーは音源をまたいでユニークになるようなものとし、1の1、1の2、またはC1-1、C1-2といった体系で、カット推奨という見出しの前に持ってきてください」 |
| D-005 | カット済み区間は、会話相手の発言カットと通常カットを見分けられるように表示する（当初はティール/青の色分け→D-008 でグレー濃淡＋「会話相手」注釈に変更）（2026-07-26） | どのカットが機械的な話者ルール由来かが一目でわかる | 「カット部分に関して、他の人の発言はそれがそれとわかるような表示の仕方をしたい。つまり、通常のカットと別の人の発言のカットは色などを分ける」 |
| D-006 | 発言ブロックの頭に時刻（動画内時刻＋元音源時刻）を表示する（2026-07-26） | 「①の2分30秒のここをカット」のような時刻ベースの指示をしやすくする | 「指示しやすいように、その音源の何分何秒目なのかを頭に出してほしい。それによって、例えば、パート1の、または大学はメディアであるの。2分30秒のここの部分はカットといったような指示がしやすくなる」 |
| D-007 | オーナー評価: セグメントごとに★5段階を見出し直下に表示し、根拠となったユーザーの言葉と日付を併記する（理由を述べた時だけ。星数の直接指定は根拠に書かない）。目安: 使えない=0〜1 / まあまあ=3 / いい=4 / かなり良い=5。★が高い順に並べ替え（未評価は★1.5相当）。分割したら評価はリセット。保存先 `data/<ID>/ratings.json`（2026-07-26） | チェック中の評価をメモでき、良いものから上に並ぶ。言葉を併記するので星の根拠が後から追える | 「オーナー評価という項目を作って、見出しに近い場所に書いてほしい。…星の数で5段階で表示する。…私のいい悪い、使えないかなり良いなどの言葉は、その根拠としてわかるように併記しておくように」「星が高いものが上の方に来るように」「一度評価したものを分割する場合があり得ます。その場合は一度評価はリセットします」「（直接指定は）意味のない情報だから書かなくていい」 |
| D-008 | 確認サイトはモノトーン基調: カット済み＝ごく薄いグレー（読み飛ばし用）、カット推奨(未決)＝中間グレー（まだ読む対象）、会話相手カット＝#575757・注釈は「会話相手」のみ、残す(判断済)＝#5d5d5d のただのグレーテキスト（○や下線なし）、カット指示（確定済み）＝グレーラベルのみで理由なし（オーナーが理由を述べた時だけ note 併記）、時刻・詰めマーカー＝グレー（詰め済みは消した確定と同じ濃さ）、象徴的セリフ＝色なし太字、見出し＝「N」のみ、✂アイコン・「カット例」・凡例・説明書き＝全廃（2026-07-26） | グレーの濃淡＝「どれだけ読む必要があるか」を表す。説明しなければわからないデザインにしない | 「青で書かれている部分は全部グレーの…グレーの濃さによって完全にカットされたか、それともカットされるべき候補なのかということを表現してほしい」「#575757 会話相手だから削除したものはこういうグレーでいい」「ハサミのアイコンはほとんど意味がないので消して」「この説明はがっつりいらない。説明しなければわからないという点で、デザインが悪いのでダメ」ほか一連の指示 |
| D-009 | 確認サイトの各発言ブロックの頭に `[セグメント番号-ブロック連番]`（例 [6-3]）を表示する。番号の前半は表示順ではなくセグメント番号（★並べ替えで順位が変わっても番号が変わらない）（2026-07-26） | 「[6-3] をカット」のようにブロックを特定した指示ができる | 「どこの部分というのを非常に示しづらいので、それぞれのブロックにナンバーなどを振るべきである。1の1とか1の2とか。 [1-1] 正しいっていうのは こういう感じで」 |
| D-010 | 要約はAI候補段階のものを使わず、編集で区切り（分割・結合・境界・大きなカット）が変わるたびに、その時点の切り出し内容から作り直して `segments.json` の `summary` に保存する。文体は「メイン話者の論旨だけを単純にコンパクトに」。レビューは表示しない（「要約:」ラベルも付けない）。目次では要約をハイライト原文の上に表示（2026-07-26） | 区切りが変わると内容のまとまりが変わるため、初期要約は実態とズレる。要約だけで目次から内容を理解できる | 「このように純粋にメイン話者の論旨を単純にコンパクトにして要約してほしい」「レビューは全くいらないのでレビューは消してくれ。…見出しにも要約を書いてください。ハイライトの上で」「最初の要約を無視して、編集で分類が変わるごと、または分類がなされたごとに要約を作るのがいいかもしれない」 |
| D-011 | 本ファイルに「オーナー指示原文」列を設け、決定の元になったオーナーの言葉をそのまま残す（2026-07-26） | 要約による解釈のズレを検証できる。指示の記録がどこにあるか一目でわかる | 「DECISIONSに オーナー指示原文 という列を作って書け これまでの分も」 |
| D-012 | 確認サイトに dark/light 切り替えを追加（右上ボタン・localStorage保存）。既定は従来のダーク。ライトでは「沈む＝薄い」の方向を反転させる（削除確定の #575757 系→ライトでは淡いグレー）（2026-07-26） | どちらの背景でも「グレーの濃淡＝読む必要の度合い」の意味が保たれる | 「dark/lightを切り替えられるようにして もちろんグレーの濃さは反転しなければいけない。今のダークのレイアウトはそのまま残して。ライトモードを追加して」 |
| D-013 | 編集の初期状態では会話相手判定を含む全カット候補をオープン（未決）にし、勝手にカットしない。各未決箇所に「カット」「残す」ボタンを付け、オーナーが押すと確定して cut_decisions.json と segments.json の drops に反映される（動画は次回 render で反映）。表示は 確定＝見えにくいグレー / 未決＝分類色: カット推奨=#e7a7bc、会話相手=#30d8ff（ラベルも本文も）、その他分類=紫系、事実確認=#c9eb00。下線・取り消し線・背景帯などの線は一切引かず、色だけで判別する（2026-07-26） | 話者分離の判定が間違っていることが多く、自動カットは本人の発言を失うリスクがある。オーナーのワンクリック判断に集約する | 「会話相手と書いてあるけど、実際にはメイン話者の場所が結構ある。…この編集の最初の段階では、すべてカットすべきかどうかはオープンになっているようにしてくれ。勝手にカットにはしない。そしてそれぞれの場所にカット、カットしないというボタンをつける。オーナーはそれをただ単に押して判断する。押すと確定する。確定されたものは見えにくいぐらいになる。判定すべきものは、白でもグレーでもない色付けをする」「会話相手は #30d8ff ラベルもテキストも」「とにかく線を引くな。…単に色で判別できるようにすればいい」「事実確認 #c9eb00」 |
| D-014 | 「候補外」という分類を廃止し、exclude_zones はカット推奨として扱う（assign_cut_ids.py が cut_decisions.json に category=gpt で取り込み、C番号を振る）。サイト・校正用PDFの候補外表示（グレー伏せ・⬛）は削除（2026-07-26） | 分類が減り、判断は「カットか残すか」の一種類に集約される | 「候補外 は単にカット推奨に含む 候補外という分類はいらない」 |
| D-015 | 文字起こしの本文は **Whisper large-v3（mlx・fp16・非量子化）単体**を正とする。Notta の文字起こしは本文には使わない。話者ラベルのみ Notta のブロック（時刻＋Speaker N）から時刻の重なりで付与する（2026-08-05） | Whisper は句読点の打ち方など細部が発言に忠実で、Notta は加工と原文との相違が混じる。加えて Whisper は本文と単語時刻が同じ1回のデコードから出るため両者が食い違わず、Notta 文字を wav2vec2 で音声へ強制アラインする工程（単語の22.5%が使用不能になっていた。findings.md 4章）が不要になり、タイムラインUIに必要なミリ秒時刻が一貫して得られる | 「わかった。文字起こしのまとまったものを見れば、明らかにわかる。ウィスパーの方が信頼できる。ウィスパーは完全ではないが、句点の打ち方とか細部が発言忠実である。ノッタの方はまずかなり加工が入っており、ところどころ加工が入っているように見えるし、若干原文と違っているところが散見される。かなり僅差ではあるが、Whisperを使うべきだというふうに判断できる。そうすればWhisperはタイムスタンプが入っているから、細かく入っているはずなので、Whisperを使えば一貫性のあるタイムラインを構成できるだろう」 |
| D-016 | ASR は精度優先で固定する。モデルは **large-v3**（turbo を使わない）、量子化しない（mlx は fp16、whisperx 側の既定も int8 → float32）。処理時間は制約としない。チャンク分割は**残す**（実測で本体の一致率 1.0000＝精度を落とさず、幻覚ループの被害範囲を限定する保険として働くため）（2026-08-05） | turbo はデコーダ4層の蒸留版で固有名詞に弱い。int8 量子化は実測で float32 と文字一致率 98.2%＝1.8% の劣化。分割の是非は実測で判断した（findings.md 2章・3章） | 「今回は時間よりもとにかく最高の精度を重視している」「音声文字起こしはリアルタイム性はいらないので、時間かかってもいい。1時間とか2時間とか、場合によっては3時間とかかかってもいい」「チャンク分割した方が精度が下がるのであれば、なるべくチャンク分割を少なくするか、なくしたい」 |
| D-017 | Whisper の `initial_prompt`（語彙バイアス）は使わない。`scripts/make_asr_prompt.py` と `prompts/asr_prompt_instruction.txt` は削除し、`transcribe.sh` の `WHISPER_PROMPT` は既定で無効のまま残す（2026-08-05） | プロンプトの形式を4通り（名詞列・内容説明・指示文・議事録要約）試したが、いずれも捏造か反復ループを起こした。話し言葉形式だけは崩れなかったが、上限223トークンに収める過程で英単語が1語混じるだけで冒頭が「 semi」×20の反復に破壊され、書き言葉が混じるだけで話者が言っていない「私は」を挿入して書き直す。得られるのは固有名詞1語程度の改善で、D-015 で Whisper を選んだ理由である発言忠実性を壊すため割に合わない。実測は findings.md 5章 | 「いや、そんな簡単に捏造とか繰り返しが起こるんだったら、それを使わなくて、イニシャルプロンプターを使わなくていいよ。」 |
| D-018 | 文字起こしの句読点の有無は問わない。Whisper がチャンク／区間によって句読点を出したり出さなかったりするが、単語のタイムスタンプが正確である限り編集に支障はないので、対処しない（2026-08-05） | 句読点を増やそうとすると initial_prompt に手を出すことになり、D-017 の捏造・反復ループを呼び戻す。読みやすさより発言忠実性とタイムスタンプ精度を優先する | 「まず、句読点はタイムスタンプが正確についている限りはそれほど重要ではない。だからそれほど気にしなくていい。」「PDFを見た限り、Nottaよりも全体的に正確で忠実に見える。ただ、演繹とかそういう、機能とか演繹や地動説のような単語がご変換している。そのままこれを使うべきなので、サジェストPIに進めてください。」 |
| D-019 | タイムライン編集UIは独立ページを作らず、確認サイトの各セグメントの**文字起こしの場所そのもの**をタイムラインに置き換える。文字は時間軸上の座標（x = (時刻−行頭時刻)×pxPerSec）に配置し、文字間の空白がそのまま無音の長さになるようにする。バーは「残す区間」で、オーナーが切らない限り分割せず1本のまま。バーの中を VAD で緑（発話）／黄（無音）に塗り分け、黄をダブルクリックするとその無音だけ落ちる（2026-08-05） | 文字とバーが同じ座標系に乗るので「どの文字を切っているか」が一意に決まる。別ページだと画面を往復することになる。バーを最初から分割してしまうと「無音を切る」という操作自体が表現できない | 「今からやる変更は少し大きいので、戻せるようにブランチを分けた方がいいかもしれない。…このウェブサイトのインターフェース上で最終状態まで編集できることが最も効率的である」「いや、なんか多分違うのが、別の画面とか作らなくていいんだよ。既存の画面の文字起こしの下にバーが出るというか、文字起こしがタイムラインに変わるってことだよ。文字起こしのその場所がね。別のウィンドウはいらないんだよ」「一応念のため言っておくが、バーは無音区間でも配置しておくんだよ。勝手にバーを最初から分割したりしないんだよ。…例えば、緑、黄色、緑と出ていたら、黄色だけカットすれば無音区間をカットできるという仕組みだよ」 |
| D-020 | 無音検出は精度を最優先し、**pyannote/segmentation-3.0**（2023年版）を使う。処理時間は制約としない。用途はタイムライン表示であり、`docs/無音詰め方針.md` 大原則3（音響解析を使わない）とは別物として扱う（2026-08-05） | 大原則3が禁じているのは ffmpeg silencedetect の音量しきい値であり、禁止理由「ノイズフロアが高いと誤判定する」を解決するのが学習済み発話検出モデル。用途も「詰めの判断」ではなく「編集のための表示」で別 | 「どのVADを使うかは、最も精度が高いものを使え。処理時間は重要ではない。リアルタイム性は全く必要がないからだ。方針転換ではない。無音の爪とは別に、純粋に編集のための表示なので、別と考えていい」 |
| D-021 | 元音源が ALAC（Apple Lossless）の場合、ブラウザ再生用に AAC 64kbps mono の `data/<ID>/preview_audio.m4a` を別途生成し、確認サイトはそれを優先して再生に使う。元音源には手を触れない。生成は `scripts/make_preview_audio.py` で、`transcribe.sh` の最後に自動実行される（2026-08-05） | ALAC は Safari では鳴るが Chrome / Firefox は非対応で、タイムラインの再生が audio.play() の reject で失敗していた。実測: 286MB(ALAC) -> 52MB(AAC)。オーナーは ALAC で保存することが多い | 「再生できません」「ALACで音源を保存することが多いので、そのファイルは一回変換する工程を挟んだ方がいいよ。」 |
| D-022 | タイムラインの本文はメイン話者（最多話者）を既定色、会話相手を話者ごとのオレンジ〜赤系にする。会話相手のターンの頭には「Speaker N」を小さく表示する。カット済みの文字も**話者の色を保ったまま沈める**（一律グレーにしない）（2026-08-05） | 一律グレーにすると「話者が違うから切った」のか「内容で切った」のかが区別できない。`docs/編集規則.md`「Speaker1 はほぼそのまま／会話相手は基本カット」を目で追えるようにする | 「話者の色を色分けして出してくれ。つまり、テキストはメインスピーカーのテキストは黒でいいが、メインスピーカーじゃない話者のテキストは色をつけて出す。オレンジとかがいいかもしれない」「それが話者に、話者が違うからカットしたのかわからないから、カット済みのグレーもちゃんと、例えばオレンジだったらオレンジグレーとか薄いグレーとか薄いオレンジとかにして、ちゃんと分けるようにして」「というか、スピーカー2とかスピーカー3とか出した方がいい。小さく上の方にね。つまり、メインスピーカーじゃないスピーカーの会話が始まったら、そのテキストの開始付近にスピーカー2とか出すんだよ」 |
| D-023 | 校正用PDF（`scripts/make_review_pdf.py`）は今後ほとんど使わない。確認・編集は確認サイト（HTML）側を正とする（2026-08-05） | HTMLの方が作り込まれていて分かりやすく、書かれている情報もほぼ同じ。二重にメンテする理由がない | 「今、構成用のPDFを見たが、はっきり言ってHTML側で表示を作り込んでいるやつを見た方が、多分はるかに分かりやすいし、ほとんど同じことが書いてあるはずなので、もはやこの構成PDFをほとんど使わないという前提でいいだろう」 |
| D-024 | 候補のランク付けと一次編集は **Claude** が行う。GPT-5.5 Pro の API 呼び出し（`scripts/suggest.py` のAPIモード）は使わない（2026-08-05） | Claude の出力で実用に足りることを確認済み。課金してまで GPT を使う理由がなくなった。実行中だった GPT の呼び出しは停止した | 「もはやランク付けとか一時編集はClaudeでいいと思う。わざわざお金をかけてGPTブローでやる理由があんまりもうなくなってきた」 |
| D-025 | `data/<ID>/` の配置を改定する（D-002 の改定）。**直下**＝オーナーが入れたもの（元音源・元動画・Notta の PDF/txt・要約・編集メモ）、**edit/**＝人の判断が入ったデータ（segments.json・cut_decisions.json・ratings.json・cutlist.json・trim_plan.json）、**generated/**＝機械が作ったもの（transcript・vad・candidates・各種PDF・preview_audio ほか。消しても作り直せる）、**contents/**＝最終書き出し、**backup/**＝退避。「中間生成物/」は generated/ に統合して廃止（2026-08-05） | 直下が30ファイル以上に膨れて何が入力で何が生成物か分からなくなっていた。配置の定義は `scripts/idpaths.py` の1箇所に集約し、移行中は旧い場所（直下）にあるファイルも読めるようにしてある | 「まず<ID>/内のファイルが散らかっているのでフォルダ分けして整理して　私が入れたデータはそのままトップに残して edit/segments.json edit/cut_decisions.json generated/民主主義の会2-5_校正用.pdf とか」 |
| D-026 | タイムラインの「無音を一括で落とす」ボタンは廃止。かわりに ↩元に戻す/↪やり直すボタン（⌘Zと同じundo/redo）を置き、保存のたびに直前の segments.json を `edit/segments_history.jsonl`（最新200件）へ退避する（2026-08-05） | 一括操作は編集を全部飛ばす危険がある。undoはページ内で、飛んだ場合の復元は履歴ファイルからできる。黄帯ダブルクリックで無音を1箇所ずつ落とす操作は残す | 「無音を一括で落とすというボタンはなくしてくれ。最悪の場合、編集したものが全部飛んでしまう可能性があるから。そして編集した履歴を元に戻せるようにするための機能を実装できる。つまり、アンドゥができるようにしてほしい」 |
| D-027 | 確認サイトに「この編集で書き出す」ボタンを設置。セグメント単位で render.py（RENDER_ONLY=index）をバックグラウンド実行し、完了したらページに final.mp4 が出る。再生は元音源（preview_audio.m4a）のままカット区間をスキップするだけで、音源自体は書き出しまで一切加工しない（2026-08-05） | 編集（drops）はJSONに保存されるだけで音は無傷。実際に切るのは render.py 実行時、という従来構造をボタン化した | 「これを書き出すにはどうしたらいいか。書き出しボタンを作ってくれ」「書き出しボタンを押すと、その編集した状態で書き出しが出てくるのがいいだろう」 |
| D-028 | スプリット操作: ⌘D＝マウス位置でスプリット（クリック不要）、右クリック＝メニュー（ここでスプリット/ここから再生）。右クリック即実行はしない（2026-08-05） | 「クリックして位置決め→⌘D」の2段階が「効かない」ように見えていた。即実行は誤操作しやすいのでメニュー式 | 「右クリックして即スプリットはおかしい。右クリックしてメニューを出してからクリックしてスプリットならいい。しかし、コマンドDでできるようにしてくれ」 |
| D-029 | 確認サイトの書き出しは音声のみ `{ID}_{INDEX}_{TITLE}.m4a`（AAC 256kbps・aac_at 優先）。`scripts/export_audio.py` が segments.json の drops を除いた keep 区間を連結して書く。元音源は壊れフレームのある ALAC m4a より同名 wav を優先。一時 mp3 案は音質面で撤回（2026-08-07） | Podcast 配信の標準は AAC で同ビットレートなら mp3 より高音質。モノラル収録なら 256kbps で聴感上無劣化。完全ロスレスは巨大なうえ配信側で再エンコードされるので不採用。動画・字幕が要るときは render.py を使う | 「書き出しはMP4じゃなくて。{ID}_{INDEX}_{TITLE}.mp3」→「なぜmp3？ podcastに最適なのは？音の質をあえて落とす意味はないでしょ」 |
| D-030 | 他の作業者への提供は**既存EC2でのホスティング**で行う。podcast リポジトリを thinkx-system モノレポへ移し、setup_podcast.sh を追加して uWSGI アプリとして **production** に常駐（staging は停止しがちなので使わない）。production は編集専用で Claude Code・AI工程は置かない（当面）。残作業＝WSGIアダプタ（web/wsgi.py）・publish/collect スクリプト・nginx Basic認証は**モノレポ移動後に実装**し、podcast 側では先行実装しない（2026-08-07） | 追加費用ゼロ・作業者はブラウザのみ（Windows可）・編集はサーバー上のJSONに直接保存され返送不要。web/ の nginx 設定はもともとこの形を想定済み。経緯は docs/discussion_20260807_HostingForWorkers.md | 「EC2は2台の小さいサーバーで月5000円ほどかかる…完成された仕組みなのでEC2ならこれを使う以外に考えにくい」「thinkx-systemにはデプロイの仕組みが完全にある setup_podcast.sh でサイトのセットアップを一つ追加すればいいはず」「他の構成に合わせるのはpodcastをthinkx-systemに移動してからの方がいいだろう」 |
| D-031 | SNS用の切り抜きは、編集済みフル音源を新ID `<ID>（切り抜き）` にコピーして独立処理する（元IDには一切書き込まない）。パイプラインはフル版と同じ手動フロー＝編集済み音源を再文字起こし→Claude が切り抜き専用プロンプト `prompts/prompt_picks.txt`（尺10〜30秒＝1クリップ1パンチライン・冒頭3秒のフック重視・文脈不足はSNSキャプションで補う・本編への導線。当初の30秒〜3分は2026-08-24のオーナー指示で二段階で短縮され10〜30秒が最終）で候補生成→既存タイムラインUIで人間が編集→書き出し。UIはフル動画用のものをそのまま使い、変更しない。動画化（縦型・デザイン）は書き出した音源＋字幕テキストを使って Canva でスタッフが行う（2026-08-24） | 切り抜きはアクセスを稼ぐフックで、SNSから Podcast 本編へ遷移させるのが目的。本編の「長尺優先」とは基準が逆なのでプロンプトを分ける。IDを分ければ確認サイトの一覧に自動で並び、元の編集を上書きする事故が構造的に起きない。元transcriptは編集で時刻がズレるので再文字起こしが確実 | 「イメージはまず編集済みのフル音源を再び高精度文字起こしする それをClaudeで所定のプロンプトを通して切り抜き動画タイトルとフレーム候補をリストアップする」「下の編集済みフル音源は一切上書きや変更をしないようにIDを変えるべきだろう」「（切り抜き）の方がいいだろう すでにIDは日本語だし」「このパイプラインはフル動画と同じ手動でいい つまり編集UIが出るまで あとは人間が編集して書き出す UIは既存のものと同じでいい フル動画のものを変えてはいけない」「尺が長すぎる もっと短くていい プロンプトからやり直し」「尺は 10〜30秒」 |
