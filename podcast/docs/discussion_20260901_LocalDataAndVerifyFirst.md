2026-09-01 「まずローカルで確認」の指摘とデータのハードリンク複製（生ログ）

引用（太字）が発案者の発言で、原文のまま。続く地の文が Claude の応答（実際に返した内容。
冗長な部分のみ圧縮）。時系列。確定事項は thinkx-system/docs/PODCAST_TRACK.md（D-52）が正。

> **その前にローカルのthinkx-system/podcast/にdataが見えないのだがまだない？まずはローカルでサーバー起動して挙動を確認してからstagingにアップすべきでは**

指摘の通りで、data/ は git 管理外のため取り込みに含まれず、実体は旧リポジトリ
（~/Sources/podcast/data、7.3GB・12 ID）にあった。対応:

- ハードリンクで monorepo 側へ複製（同一ディスク・一瞬・追加容量ゼロ・旧環境の
  パイプラインは無傷）。実体の移動・旧側の整理はオーナー判断として保留
- 編集データ27ファイル（edit/*.json + 履歴）を git 管理に載せてコミット
- 検証サーバーをポート 8011 で起動（8010 は旧リポジトリの preview_local.py が
  8/28 から掴んだままだったため）し、実データで一覧12 ID・タイムライン7本・
  Range 配信 206 を確認。確認 URL http://127.0.0.1:8011/ を提示

（このあと 9/17 に「旧フォルダの編集の取り込み」→「完全移行・旧フォルダ凍結」→
staging 開通まで進む → discussion_20260917_StagingCutover.md）
