# thinkx-system/docs/CLAUDE_GENERAL.md
#
# Claude Code 共通規約（プロジェクトは作業ディレクトリ内で完結させる）

これはこのプロジェクト専用ではなく、**Claude Code で作業する全プロジェクトに適用する普遍ルール**。
新しいプロジェクトを始めるときは、この規約を各プロジェクトの CLAUDE.md からリンク／コピーして使い回す。

## 原則
プロジェクトに関わるファイルは、すべてそのプロジェクトの作業ディレクトリの中に置く。
ホームディレクトリや共有の場所（`~/venvs`, `~/.cache` を要求する置き方, `/usr/local` など）に
プロジェクト固有のものを散らさない。作業ディレクトリごと移動・複製・削除すれば完結する状態を保つ。

## 具体ルール
1. **Python 仮想環境はプロジェクト直下に置く／作ったら必ず requirements.txt を同梱する**
   - `<project>/venv` に作る。`~/venvs/...` には作らない。
   - 作成: `python3 -m venv <project>/venv && source <project>/venv/bin/activate`
   - **venv を作った（またはコピー／依存を更新した）ら、必ず `<project>/requirements.txt`
     を書き出す**。venv 実体（1GB超になりがち）ではなく requirements.txt を「正」とし、
     いつでも `python3 -m venv venv && venv/bin/pip install -r requirements.txt` で
     再現できる状態を保つ。venv を作って requirements.txt が無い状態を残さない。
   - 生成: `venv/bin/pip freeze > requirements.txt`。バージョンは固定（`==`）で残す。
   - Apple Silicon では arm64 の python で作る（Intel 版だとアーキ不一致でセグフォルトする）。
     複数アーキの venv を作らない。ひとつだけ、プロジェクト内に。
   - **例外: 自己完結した独立コンポーネント**（別プロセスで動く web サイト、常駐デーモン等で、
     他プロジェクトでも使い回すもの）は、プロジェクト内のサブディレクトリに置き、
     **そのサブディレクトリ内に専用の venv / requirements.txt / CLAUDE.md を持たせてよい**
     （1プロジェクト1venv ではなく 1コンポーネント1venv）。各コンポーネントは自分の
     ディレクトリ内で自己完結させ、本体と依存を混ぜない（例: `web/` に確認用サイトを置き、
     `web/venv`・`web/requirements.txt`・`web/CLAUDE.md` を独立させる）。

2. **パスは設定ファイルで、プロジェクトルート基準で解決する**
   - スクリプトは自分の位置からプロジェクトルート（`$HERE`）を求め、
     `config/paths.conf` などに `VENV="$HERE/venv"`、`DATA="$HERE/data"` のように
     ルート相対で書く。絶対パスやホーム直書きをスクリプトに埋めない。

3. **生成物・中間ファイルもプロジェクト内**
   - データ、キャッシュ、一時ファイル、モデル出力などは `<project>/data` や
     `<project>/.cache` などプロジェクト内に置く。掃除もプロジェクト内で完結させる。

4. **既定値もプロジェクト内を指す**
   - スクリプトの「未設定時のデフォルト」も `$HERE/...` にする。
     うっかりホームを掴む事故（例: 古い `~/venvs/podcast` を掴んでセグフォルト）を防ぐ。

5. **環境の健全性チェックを入れる**
   - venv が無ければ「プロジェクト内に作れ」と案内して止める。
   - アーキ不一致など、黙って落ちると原因が分かりにくいものは、実行前に照合してエラーで止める。

6. **秘密鍵・API キーは gitignore 済みの `<project>/.env` にだけ置く**
   - **tracked なファイル（起動スクリプト `rc.sh`・`setup_env.sh`、コード、config）に鍵を直書きしない**
     （push で漏れる）。`.env` と `.env.*` は `.gitignore` に入れる。
   - コードは鍵を環境変数から読む（`os.environ`）。`.env` は自動では読まれないので、セッション起動
     スクリプトで読み込む: `set -a; [ -f .env ] && . ./.env; set +a`。
   - **有料 API を使う経路は、実行前にコストを概算してユーザーに確認する**。無料の代替経路があれば
     それも提示する。黙って課金 API を叩かない。

7. **外部ツール（pandoc / lualatex 等の実行バイナリ）もシステム依存にせず、プロジェクト内に固定する**
   - **どこのツールを使うかを PATH 任せにしない**。コードは実行ファイルを PATH で探さず、
     **環境変数（例 `PANDOC` / `LUALATEX`）→ `<project>/bin/<tool>` → 最後に PATH** の順で解決する
     （PATH 直書きの `subprocess.run(["pandoc", ...])` は禁止。`_tool()` のような解決関数を通す）。
   - **毎回使うツールはプロジェクト内に実体を持つ**。`<project>/bin/` にネイティブビルドの実行ファイル
     （またはそれへの symlink）を置き、コードはそこを既定にする。どのバイナリを使ったかを固定・記録する。
   - **アーキテクチャを一致させる**（CLAUDE_GENERAL の大原則）。Apple Silicon では **arm64 ネイティブ**を使う。
     Intel(x86) バイナリを Rosetta で走らせると、環境次第で**ハング/セグフォルト**する（実例: Homebrew の
     x86 `pandoc` を Rosetta 実行→ `--version` すらハング。しかも壊れた Rosetta AOT キャッシュは
     `/var/db/oah` にあり sudo 無しでは消せず、別パスへコピーしても内容ハッシュで poison が付いて回る）。
   - **導入手順を tracked に残す**。`setup_env.sh` 等に「どの配布物を・どのアーキで・どこへ置くか」を書き、
     `<project>/bin/` を再現できるようにする（venv の requirements.txt と同じ思想＝実体でなく作り方を正とする）。
   - ネットワーク取得やシステム全体へのインストールが要るときは、**実行前にユーザーへ確認**する。

8. **大量処理は毎回「使用量見積もり＋停止予算の確認」をしてから実行する（全プロジェクト適用）**
   - 実行開始直後（大量処理を起動する前）に、**使用トークン総量 → Max プランのおおよその窓数**を概算して提示する。
     **窓数はトークン概算の2倍の消費量で計算する**（安全係数2。実測: Fable5 は見かけのトークン数より
     早く上限に達する。オーナー指示 2026-07-16）。
     ドル・API換算は出さない（Max 定額では追加費用が発生せず紛らわしいだけ）。枠の残量%とリセット時刻は
     セッションから読めない（`/status` のみ）。
   - **1窓に収まる見込みなら確認不要**でそのまま実行してよい。
   - **1窓を越えると分かった時点で必ず一文で確認する**:
     「**◯◯すると Max プランで概算◯〜◯窓使います。どうしますか？**」
     選択肢は必ず3つ: **①全部進める（上限到達で自動停止→次窓で残りだけ再開）／②1/2窓で止める／③1/3窓で止める**。
   - 選ばれた割合に達したら**再開可能な状態で一旦停止**して再判断を仰ぐ。そのため処理は必ず**チャンク単位で
     プロジェクト内にキャッシュ**し、どこで止まっても「残りだけ再開＝ゼロ手戻り」にする。
   - 終了前に実測に近い使用量を振り返る。

9. **新規にファイルを作成する場合ルートからのパスを最上部に必ず書く。一行空白行を開けてからそれが何のファイルなのかを一行で簡潔に書く。**
   - 例. thinkx-system/docs/CLAUDE_GENERAL.mdの場合 
   ```
   # thinkx-system/docs/CLAUDE_GENERAL.md
   #
   # General rules for every Claude Code projects.
   #

   このファイルにはプロジェクトによらないClaudeCodeの汎用的な規則が書かれている。
   ```
   -  それによってVSCodeで人間がファイルを開いたときにファイルのプロジェクト内の位置が即座にイメージできる．通常開くときに単にファイル名で検索して開くため位置関係まで調べないとわからないからである．


## なぜ
- 作業ディレクトリを消せば環境ごと消える＝後片付けと再現が確実。
- 別マシンや別パスへ移しても、ルート相対なのでそのまま動く。
- ホーム共有の壊れた環境を誤って掴む事故が起きない。
- 複数プロジェクトが互いの環境を汚さない。

## このプロジェクトでの適用状況
- venv: `<project>/venv`（Python）。依存は `requirements.txt`（anthropic / requests / pyyaml / pillow）に固定版で同梱。
  再現は `python3 -m venv venv && venv/bin/pip install -r requirements.txt`。
