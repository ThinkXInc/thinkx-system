[uwsgi]
# socket
socket = /tmp/uwsgi_kazukiotsukacom.sock
# pid file
pidfile = /tmp/thinkx.pid
#permissions for the socket file
chmod-socket = 666
# application root
chdir = ../web-server
# virtual env (relative path from application root)
venv = ./venv
# main.py
module = main
# flask app variable name
callable = app
# master
master = true
# maximum number of worker processes
processes = 1
# threads
#threads = 2
threads = 1  # for debug
# clear environment on exit
vacuum = true
# each worker will load the trained model for them self and not share with others
#lazy-apps = true  # remove comment out for deploy !!
# autoreload
py-autoreload = 1
# maximum upload file size
limit-post = 70000000
# Increase buffer size otherwise large request is excluded
buffer-size = 8192
# load uWSGI plugins TODO: python39
# export PYTHON=python3.9 && uwsgi --build-plugin "/usr/src/uwsgi/plugins/python python39" && mv python39_plugin.so /usr/lib/uwsgi/plugins/python39_plugin.so && chmod 644 /usr/lib/uwsgi/plugins/python39_plugin.so
#plugins-dir=/usr/lib/uwsgi/plugins/
##plugins = python36
# log file
# daemonize = /var/log/uwsgi/uwsgi.log  # <- don't use this together with systemd