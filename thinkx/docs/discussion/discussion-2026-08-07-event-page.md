# thinkx/docs/discussion/discussion-2026-08-07-event-page.md
#
# イベントページ(/event/philsemi2609)の文面・レイアウト調整の議事録(2026-08-06 深夜〜08-07)

前日分は `discussion-2026-08-06-event-page.md`。本書はその続きで、経歴の書き換えから
ヘッダーの試行錯誤、「こんな人に」の追加、URL 変更、staging/本番手順までを扱う。
オーナー発言は原文のまま。Claude 側は要点に圧縮。

---

## 1. 経歴の書き換え

**オーナー**:
> 今日追加した、昨日今日で追加したイベントのページがあるんだけど、私の経歴を次のように書き換えて。
>
> 大学卒業時に独立、現在主流のクロール型ニュースリーダーを日本で初めて普及、半年で2万5千ダウンロード、スーツケース一つで世界を放浪、サンフランシスコでハッカーハウス運営、国内初となるAI活用型の投資用不動産分析システムを企画・開発(2025年にM&A)、独自の言語意味解析機械学習アルゴリズムや音声アルゴリズムで複数の特許を保持。
>
> この同じ部分を同じように書き換えと。

**Claude**: 対象は `views/templates/event/philsemi2609.html` の1ページのみ(`deepsocietyclub3.html`
は改名前の旧名で同一)。差分は不動産システムの記述だけで、
「世界初の本格的な業務用不動産投資分析AIシステム企画開発」→「国内初となるAI活用型の投資用不動産
分析システムを企画・開発」。他サイトを含め同一経歴文の重複は無いことを検索で確認(`locales/history.json`
のハッカーハウス言及は会社沿革の別文章なので触らない)。

## 2. ヘッダーの試行錯誤(最終的に版指定で巻き戻し)

**オーナー**(スクリーンショット添付):
> このようにヘッダーからタイトルを消して
> ----------------
> 「ファーブラー・メソッド」
> 出版決定記念
> -----------------
> として枠の中に二行　上の行は小さく

**オーナー**:
> サロンと懇談会（立席形式　生ピアノ演奏付）
> にヘッダを調整してみてくれ

**オーナー**:
> やはり
> `<p class="hero-title" ...>「哲学とAI─システムに全てが回収される時代にどう豊かに生きるか」</p>`
> を元のレイアウトでヘッダーに出し / 「ファーブラー・メソッド」出版決定記念 はそのままで /
> サロン->シンポジウム に戻し / `<h1 class="event-title">` をhideにし（まただせるように）

**オーナー**:
> ヘッダー内の連続起業家、独立計算進化学者、現代芸術家がタイトルと重なっているので直して

**Claude**: 実測(iframe で幅を変えて計測)したところ、重なりはウィンドウ幅に依存。1440px では
余白 60px 以上あるが 1250px を切ると肩書に食い込む。中間ブレークポイントと予約幅で回避を試みる。

**オーナー**:
> 哲学とAIはこれくらいでかく / サブタイトルをもっと小さく　最初のレイアウトみたいに二行でいいだろう

**オーナー**:
> `<p class="hero-title">哲学とAI<span class="hero-title-sub">─…</span></p>`
> こういう構造は別に調整しにくいからやめろ
> テキストブロックごとにブロック
> `<div><p> ... <span> ... </div>`

**オーナー**:
> ビジネスの上の領域はもう少し詰めてヘッダーの高さを僅かに高くすればもう少し余裕がでるのでは

**オーナー**:
> というかなんでタイトル群が中央じゃないんだよ　馬鹿か

**Claude**: 予約幅を左右非対称(右だけ)に取ったため中央からずれていた。実装ミス。左右対称に修正。

**オーナー**:
> 単に大塚一輝ブロックを左上にすれば重ならないようにできるだろ

**オーナー**:
> スピーカー位置はこれでよかったんだよ / 単に少しずらして重ならないようにすればよかったんだよ

**オーナー**:
> 馬鹿か　おかしいだろそれは
> バランスが変だから　スピーカーと中央のタイトル群の配置バランスを戻せよ

**オーナー**(元レイアウトのスクリーンショット添付):
> こういうバランスだっただろ
> もうこれでいいよ / タイトルはヘッダにいれなくていいよ /
> ファーブラー・メソッドを追加するのと　シンポジウム/の部分だけ / それ以外はこのバージョンに戻して
> 出版決定記念 も必要

**Claude**: コミット `0ebb2d5` の版を取り出して丸ごと戻し、そこへ2点だけ載せ直した。
①金枠を2行(上に「出版決定記念」22px、下に小さく「ファーブラー・メソッド」13px、div + p 構成)
②イベント種別を「シンポジウム/懇談会（立席形式　生ピアノ演奏付）」。
ヘッダーのタイトル行は入れず、本文の `<h1>哲学とAI</h1>` は表示のまま。試行錯誤で入れた
vw スケーリング・中間ブレークポイント・肖像の移動はすべて消えた。差分 13行追加・24行削除。

→ この経験は GUIDELINES「元の版に戻して、差分だけを載せ直す」「テキストは1ブロック1要素で組む」
として常設化した。

## 3. 文字サイズの指定(オーナーが devtools の値を直接指定)

**オーナー**:
> `.hero-event-type { font-size: 23px; }` / `.event-title { font-size: 56px; }` /
> `.event-title-sub { font-size: 29px; }`

**オーナー**:
> `.hero-date { font-size: 17px; }` / `.portrait-titles { font-size: 12px; }` /
> `.portrait-name { font-size: 20px; }` / `.highlight-text { font-size: 18px; }` /
> `.profile-lead { font-size: 18px; }`

**Claude**: 指定値をそのまま適用(他プロパティは触らない)。実測メモ: 種別 23px により
1250px 未満では肖像の肩書と重なる。本文サブタイトル 29px は本文カラム 720px に収まらず2行になる。
広いウィンドウでは問題ないため現状維持とした。

## 4. スマホのレイアウトとプロフィール順序

**オーナー**:
> スマホのレイアウトおかしいだろこれは / まず出版記念 / 次にシンポジウム / 次にタイトル / が読めないと
> 左上にスピーカーはないだろ

**オーナー**(後に再指摘):
> 治ってなくないか？

**Claude**: 一度直したが、上記2の「版指定で巻き戻し」の際に一緒に戻ってしまっていた。
`order: -1` と左上マージンを外し、スピーカーを文言の下・中央に配置。実測順序は
帯 → 種別 → 日付 → スピーカー → 本文タイトル。

**オーナー**(プロフィールのスクリーンショット添付):
> この順序にして

**Claude**: 名前 → リード → 肩書 → 学問の段落 → 経歴の段落 → 歴任リスト の順に並べ替え
(本文は1文字も変更していない)。

**オーナー**:
> 2026年NTTドコモビジネス(株)AIクラウド事業アドバイザー。

## 5. 「こんな人に」ブロックの追加

**オーナー**:
> こんな人に / ・マネーゲームがどのように変わるのか知りたい。/ ・本当の哲学とは何かに興味がある。
> 「知的関心の高い人のメンサ」哲学サロンコミュニティに興味がある。/ ・AIツールでプロがどう生産性を
> 倍化しているのかに興味がある。
> このブロックをスピーカーの上に追加してくれ。レイアウトをいい感じにしてくれ。

以降、文面は4回改稿(マネーゲーム→ビジネスのルール、コミュニティの列挙の仕方、
AIツール→ClaudeCode、語順)。最終形は下記。

**オーナー**:
> このブロックは背景を薄グレーにして少し浮き立たせてほしい。/ ForYouという見出しはいらない。

**オーナー**:
> 浮き立たせるというか、少しこのブロックだけが他とちょっと違うような風になっていればいい。
> ・ はダサいからつけるな。

**オーナー**:
> 箇条書きリストをもう少し別の形で示せ。123という番号を振って、四角い枠で囲うというのがいいかもしれない。

**オーナー**:
> 四角い枠というのは、つまり数字が白抜きになっているということ？角丸の四角で囲まれた数字ですね。数字はちょっと太くします。

**オーナー**:
> こんな人には中央揃え。このブロックは明朝体じゃなくてコシック。

**オーナー**:
> やはりゴシックはやめよう。品がない。

**Claude**: 最終形は、薄いグレージュ(#f1ede5)の面・影と枠線なし、見出し「こんな人に」中央揃え、
番号は CSS カウンタで振り角丸(7px)の金枠に太字、書体はページと同じ明朝。
`FOR YOU` ラベルと罫線は削除。

## 6. URL の変更

**オーナー**:
> event/philsemi2609.html でなく event/philsemi2609 にして

**Claude**: `main.py` のルートとログ、ゴールデン `tests/golden/route_sweep.json`、`og:url`、
ローカル起動手順の4箇所を同時更新。ローカルで新 URL 200 / 旧 URL 404 を確認。
pytest は `/filedrop` の既知1件(F-E17・staging 以外では必ず落ちる)以外 green。

**オーナー**:
> https://staging.thinkxinc.com/en/event/philsemi2609 → 404

**Claude**: 仕様どおり。このページは独立ページとして language_wrapper を通しておらず、
`/<lang>/` 付きルートを登録していない(F-E20)。本番でも同じ。必要なら1行足す。

## 7. ローカル確認環境

**オーナー**:
> ローカルで確認できるアドレスをくれ / ローカルサーバーを起動する手順をマニュアルにしろ
> http://127.0.0.1:5000/event/philsemi2609.html 画像が出ていない　なぜ

**Claude**: 画像・CSS・JS は本番/staging では nginx が配信していて Flask にルートが無いのが原因
(F-E19)。`main.py` の `if __name__ == '__main__':` 内にだけ開発用の静的配信を追加した
(uwsgi は `__main__` にならないので本番の配線は不変)。起動手順は
`thinkx/docs/ローカルサーバー起動手順.md` に新設。`web-server/` をカレントにしないと
TemplateNotFound になる点も明記。
