# 設計決定の記録(DECISIONS)

この文書は 2026-07 の infra(I トラック)作業から**確定した決定だけ**を蒸留したもの。
経緯・却下案は含まない(指示の原文は GUIDELINES.md、気づき・問題は findings.md に保存)。
**実行時の規範は常に各計画書・各正本文書**であり、本書と食い違う場合はそちらが優先する。
時系列に並べる。決定が覆った場合は新しい行を追記し、旧行に(→D-xx で更新)と記す。

| # | 決定 | 一行根拠 | 規範文書 |
|---|---|---|---|
| D-1 | 拠点グローバル IP の取得は dig(OpenDNS/Google DNS)で行う | curl/wget は settings で deny 済み | docs/step1-rehearsal.md |
| D-2 | EC2 キーペア名 = supercom(東京)。既定 RSA | aws-cli v1 は --key-type 非対応 | docs/step1-rehearsal.md |
| D-3 | terraform は tfenv で導入し 1.15.8 を exact ピン。自分が読むファイルへのリダイレクト(`terraform version > .terraform-version`)禁止 | 依存 exact ピン原則。`>` が先に空 truncate して版解決が壊れる実害 | docs/step1-rehearsal.md / terraform/.terraform-version |
| D-4 | インフラ変更のたびに料金概算を返す(静的料金表・HOURS/JPY_RATE・停止考慮) | 「毎回…料金見積もりが返却されるようにしたい」 | scripts/README.md(cost-estimate) |
| D-5 | apply 前に構成図+変更+料金を自動で出す。図=模範 diagram.md(変えない)。追加=緑/変更=黄/削除=赤を**リソース単位でブロック全体**に。変更なしは普通に表示。検出は terraform plan(git 差分ではない) | 「どうインフラが作られたり変更されるのか明確に把握」「いちいち指示しなくても自動で」 | scripts/README.md(plan-summary) / runbooks/diagram.md |
| D-6 | doc は kaz スタイル(〜ならOK 等)。OK/NG 集は解釈を加えず前後の文のみ記録 | 簡潔・自己完結の表現を意図して選んでいる | docs/conventions/DOC_STYLE_GUIDES.md |
| D-7 | 気づいた点は随時 findings.md に書く(一般規則) | セッションは使い捨て、状態はファイルが持つ | findings.md / CLAUDE.md |
| D-8 | GitHub 認証 = Deploy key(repo 単位・read-only)・手動ブラウザ登録・自動化しない。clone は host 別名(git@github-<repo>:) | EC2 は「取得して流すだけ」で write 不要。被害が repo 単位 read-only に閉じる。自動化は部品6つ増で割に合わない | docs/github_deploy_key.md |
| D-9 | EC2 実行ユーザ = RUN_USER(既定 kaz)で統一。鍵/config は /home/kaz/.ssh。手作業は sudo -u kaz -H + 絶対パス | 「今kazで動いているなら無理に変える必要はない」。service User=kaz・/src 所有と整合 | docs/user_setup.md |
| D-10 | setup_*.sh = raw doc をほぼそのまま実行ファイル化。上から実行するだけの linear(if/for/関数/set -e なし)。将来ほぼそのまま Dockerfile 化 | 「bashの分岐などがあるとそれができない」 | scripts/README.md / GUIDELINES.md |
| D-11 | ファイル名 = setup_user.sh / setup_webserver.sh / setup_loadbalancer.sh。smoke 系は削除。setup_supercom3.sh は見本で変更禁止 | setup_ = 元 doc に対応する命名 | setup/ |
| D-12 | オーナードラフトの変更は許可制。理由は 現状/問題/修正案/理由 で丁寧に書く | 「私のドラフトから変更する場合は許可を仰ぐように」 | GUIDELINES.md |
| D-13 | quantz-web 依存の処理(clone 含む)は setup_quantz.sh に分離。setup_webserver.sh は quantz-web と独立した設定のみ | webserver は OS/ミドルウェア環境、app は別段 | setup/setup_quantz.sh |
| D-14 | .env は秘密なのでスクリプト・git に書かない。真実は **`<site>/.env`(サイト clone ルート・各 repo が .gitignore 済み)**= デプロイ先 `/src/<site>/.env`(config.py の PRJ_ROOT/.env)の鏡。**`push_env.sh`(.env)と `push_assets.sh`(video)は分離・各々別実行**(何をしているか明確に)。setup も別ステップで配置(.env は 640・無ければ WARN)。certs/deploykeys は push_secrets(secrets.tgz)側。旧 infra/env/ 集約は廃止 | 「.env を infra/env でなく thinkx/.env のようにするのが自然」「.envと assets の転送は分離し各実行されるべき、何をしているか分かりやすい」。config.py が PRJ_ROOT/.env を読む | setup/*.sh / etc/push_env.sh / etc/push_assets.sh |
| D-15 | Python は 3.9.6 ソースビルド(3.10 にしない) | 全 raw docs が 3.9.6。「ソースビルドは経験上必須 コマンドが常に決まっているから」 | setup/setup_webserver.sh |
| D-16 | staging の LB は ssl_certificate パスに自己署名を生成して nginx を起動可にする(prod は certbot --dns-route53 + cron) | 証明書不在だと nginx -t が失敗し LB が立たない(loadbalancer/nginx.conf 実測) | setup/setup_loadbalancer.sh / findings F2 |
| D-17 | bash 規約: 観測系は set -e/exit 禁止(関数+return)、変更系は set -euo pipefail。setup_*.sh は D-10 の linear を優先 | source 時に呼び出し元シェルを殺す実害(実測) | docs/coding_guides/bash.md |
| D-18 | infra は独立リポジトリ(ThinkXInc/infra)・ブランチ 2026refactor に push | ワークスペース規約(全リポジトリ統一ブランチ) | CLAUDE.md |
| D-19 | ツール要件は scripts/README.md に「ゼロから再実装できる粒度」で集約 | 「status.shをゼロから書こうと思ったらそれを見れば作れるようなものを」 | scripts/README.md |
| D-20 | 指示は GUIDELINES.md(原文維持・解釈分離)、決定は DECISIONS.md(時系列)に記録 | 「一度指示や決定したことを2度繰り返さない」 | GUIDELINES.md / 本書 |
| D-21 | run/stop/restart は infra/run/ に置き、repo 内スクリプトへ委譲せず systemctl 等の実コマンドを直接書く | 「デーモンの起動などはこのinfraレベルが握っているのであってquantz-web側でないだろ」 | run/ / GUIDELINES.md |
| D-22 | certbot --dns-route53 の Route53 権限は terraform の IAM ロール(instance profile)で LB に付与する。「自動化必須」。EC2 にアクセスキーは置かない。※supercom-terraform-iam は将来さらに狭める可能性を留保(Permissions Boundary で supercom-* ロールに書ける権限の上限を強制する等。STEP2 ハードニング候補) | それまで EC2 は IAM 権限ゼロで --dns-route53 が動かない状態だった。CLAUDE.md #6 と整合 | terraform/iam.tf / docs/SECURITY.md |
| D-23 | terraform の実行者は IAM ユーザ supercom(inline 2本: ec2:* 東京限定+非 supercom タグの削除 Deny / iam を role・instance-profile の supercom-* に限定 + TagInstanceProfile 含む)。transcript-deployer(出自不明・過剰権限)は移行確認後に削除 | 旧ユーザは S3FullAccess 等が広すぎ、iam:* も無く 403。名前も意味不明 | findings.md / docs/SECURITY.md |
| D-24 | setup_*.sh 冒頭に `export NEEDRESTART_MODE=a`(apt の needrestart 対話を抑止) | 対話が出ると「上から流すだけ」の無人実行が止まる(実測) | setup/*.sh |
| D-25 | TLS 発行は staging でも certbot --dns-route53 の実発行(--test-cert)でリハーサルする。更新は apt certbot 標準の systemd timer(無人)+ nginx reload の deploy hook。cron 案は timer と二重のため不採用 | --test-cert はレート制限を消費せず本番と同一チェーン。2026-07-12 に thinkxinc.com で実証済み | setup/setup_loadbalancer.sh / findings.md |
| D-26 | web nginx(nginx-web-root)のログは stdout/journal でなく /var/log/nginx/{access,error}.log へ出す(ltsv 維持)。原本 quantz-web は stdout だった | trace.sh が LB/web を同じ方法(ファイル grep)で層別追跡できる。logrotate は nginx パッケージ同梱が面倒を見る | nginx-web-root/nginx.conf / infra/etc/trace.sh |
| D-27 | 切替順序: まず静的サイト(thinkx / kazukiotsuka.com / transformism.art 等)を nginx-web-root 構成で動かし稼働確認する。quantz-web への変更(conf.d 整理・include 有効化)は**新サイトの稼働確認後** | 「これは新サイトが稼働確認できてからすべきでは」 | nginx-web-root/nginx.conf / 本書 |
| D-28 | LB→web の向き先は IP ハードコードをやめ内部 DNS(Route53 private zone・terraform が A レコード管理)。LB nginx は `resolver 169.254.169.253 valid=30s` + 変数 proxy_pass で TTL 追従(reload 不要)。内部名は公開ドメイン(5つ以上)と独立で 1 台 1 名(→命名は D-31 で更新) | 「ハードコードが間違い 動的にする必要がある」。IP の真実は terraform に一元化 | terraform/dns.tf / loadbalancer conf.d |
| D-29 | web の EIP 化と同時に staging も EIP(lb/web とも全 env 固定 IP) | 「stagingは頻繁にアクセスするのでIPが変わると面倒」。IPv4 課金は自動割当でも EIP でも同額 | terraform/instances.tf |
| D-30 | setup(環境)と配線(daemon 登録)は分離する。nginx-web-root の配線は setup_webserver に足さず独立の setup_nginx-web-root.sh(1 script = 1 repo)。デーモン管理は supervisord ではなく **systemd**(quantz-web repo に supervisor 設定は存在しない — 全検索で確認。supervisord は Transcript Scraper 機の手法) | 「setupは少なくとも配線と独立しているべき」 | setup/setup_nginx-web-root.sh / findings.md |
| D-31 | 内部 DNS の命名 = zone **supercom.internal**、レコード **web1.supercom.internal / lb1.supercom.internal**。加えて VPC の DHCP オプションで search domain=supercom.internal を配布し、シェルでは `ssh web1` / `curl web1:8005` の短名で運用(FQDN は設定ファイル内のみ) | .internal 直下に自組織ラベルを挟む定石(ICANN 予約)。supercom はクラスタ名として実態に合い、将来 web1.staging.internal 等に拡張可 | terraform/dns.tf |
| D-32 | プログラム的なもの(分岐・ループ・状態)が必要なら bash でなく **python** で書く(`scripts/`=py の慣例と一致) | 「bashは読みにくくエラーを頻繁に出すからだ」 | GUIDELINES.md |
| D-33 | Deploy key: 真実は Mac の **infra/deploykeys/**(.gitignore)。鍵生成は repo 追加時に Mac で1回 → .pub を GitHub 登録(最初で最後)。setup 前に tgz で配る(`tar czf secrets.tgz -C infra certs deploykeys` + scp)。**check_deploykey.py <repo> を setup とは別の独立コマンドとして setup の前に実行**(鍵を .ssh へ install=上書き冪等・config.d alias 生成・`ssh -T` 検証)。出力の**最終行が結果**(OK か、登録すべき公開鍵)。setup 本体には埋め込まない ── 埋め込むと check 失敗時に後続(clone/nginx)が走って肝心のメッセージが埋もれる。EC2 再作成でも scp+再実行で復元、GitHub 再登録不要。別名 = repo 名 | 「人間が設定したか忘れるとハマる」「keyは保管し検出して警告・吐き出す」「exitは ssh が切れるから使うな」「コレガ末でないと何をすればいいかわからない、ログを追わない」「a の全 repo 再登録は非現実的」 | setup/check_deploykey.py |
| D-34 | 保管場所は **infra/deploykeys/**(infra/secrets/deploykeys でなく)。理由は「隠す」でなく**階層が浅く見つけやすい**運用上のもの(ディレクトリ名での秘匿=security by obscurity は無効。守りは .gitignore と Deploy key の read-only)。certs も infra/certs/ でフラットに並ぶ | 「secretsはここに秘密があると自ら告知しているようなもの 階層が深いのは見つけにくい」 | infra/.gitignore / infra/deploykeys/ |
| D-35 | ロジック(分岐・検証・エラー処理)は bash に書かず **Python に切り出す**。外部依存(Deploy key 等の人手作業)は文書に前提として書くのでなく、script 冒頭の **preflight(check_deploykey.py)で機械的に検証**し、不足時は必要情報(公開鍵等)を表示して停止する | 「bashは読みにくくエラーを頻繁に出す」「人間が忘れるとハマるのは構造欠陥」(D-32 を preflight 方針まで具体化) | setup/check_deploykey.py / GUIDELINES.md |
| D-36 | setup_ **および run/(run/stop/restart)** の末尾に `# verify` を置き、本質的な成功条件を判定して**色付き**(緑=OK/赤=FAIL/黄=WARN)を出す。加えて冒頭に何をするかの echo。ログを追わず色だけで成否が分かるように。Dockerfile 移植可のため printf リテラル ANSI + インライン `&&/||`(if/for/関数/set -e/exit なし・echo -e 不可) | 「成功したのか失敗したのかよく見ないとわからない。緑/赤/黄で。Dockerfile移植可」「これだと何が起きたのかまるでわからない 最低限ログは出すべき」 | scripts/README.md / setup/*.sh / run/*.sh |
| D-37 | uwsgi は SIGTERM を reload 扱いするため systemd の stop/restart がハングする。systemd drop-in `KillSignal=SIGQUIT` + `TimeoutStopSec=10` を setup_<site> で置く(uwsgi は SIGQUIT で即 shutdown)。原本側の正攻法は uwsgi.ini `die-on-term=true`(S トラック) | restart が 90 秒ハングし run スクリプトの journal 出力に到達しなかった(実測) | setup/setup_thinkx.sh / setup_kazukiotsukacom.sh |
| D-38 | staging 公開は **staging.<domain>** の A → LB EIP を **Route53 コンソールで手動追加**(terraform 化しない・apex 無変更)。閲覧は **Basic 認証**で保護(htpasswd は `env/<host>/.env` の user/pass から setup 時に openssl 生成・**chmod 644**=nginx worker が読める)。証明書は既存ワイルドカード(lb-certs.tgz 由来)を流用 | 「staging.thinkxinc.com で確認」「basic auth をかけるべき」「DNS は手動」。640 だと nginx が htpasswd を読めず 500(実測) | Route53(手動)/ setup/setup_loadbalancer.sh / loadbalancer repo |
| D-40 | 動画等の **git 管理外の実アセット**(`<site>/web-server/views/video`)は `etc/push_assets.sh <host> <site>…` で配布し、**setup_<site> が展開(無ければ黄 WARN)**。ec2→setup→run の流れに組み込み、忘れると silent に「動画なし」でなく WARN が出る。真実は workspace のサイト clone(ローカル・gitignore)、重複コピーしない。大物なので secrets.tgz とは分離 | 「転送スクリプトが必要だが忘れると動画がない、でなく ec2→setup→run の流れに自然に組み込まれているのがいい」 | etc/push_assets.sh / setup/setup_<site>.sh |
| D-39 | LB→web の backend は **loadbalancer repo の `2026refactor` ブランチ** conf.d に `web1.supercom.internal`(内部 DNS・literal hostname)でコミット。**master はオンプレ本番設定(192.168.1.8)で不変**(他 repo と同じくブランチ分離)。setup_loadbalancer は clone 後 `git checkout 2026refactor`。cert は push_secrets の secrets.tgz 同梱 `certs/lb-certs.tgz` を展開 | loadbalancer master=オンプレ本番 LB の生きた設定。AWS 変更を master に載せるとオンプレが pull 時に 502。全 repo 2026refactor 規約に従う | loadbalancer repo(2026refactor) / setup/setup_loadbalancer.sh |
| D-41 | run/stop/restart は **サイト/サービス単位**(host まとめない)。命名は deployable unit = setup_/repo 名に一致(`restart_thinkx` / `restart_nginx-web-root` / `restart_loadbalancer` 等)。日常操作は生コマンドでなく **run/ スクリプト経由**にし、順序・journal・色 verify 等のナレッジをそこに蓄える | 「restart_webserver ってなんでまとめて restart しようとする？サイトごとだよ」「生コマンドでなく run/restart_ スクリプトを使おう、ナレッジを蓄えられる」 | run/*.sh |
| D-42 | 他リポジトリ(loadbalancer / 各サイト)の変更は infra から push しない。**私が workspace clone で編集し diff を見せ、commit/push はオーナー**。AWS 用変更は master でなく **2026refactor**(master=オンプレ本番の生きた設定・不変) | 「loadbalancer は今本番で動いているが master に push？2026refactor でなく？実機は自動で変わらない」。CLAUDE.md 禁止(他リポジトリのコード変更)と整合 | CLAUDE.md / GUIDELINES.md |
| D-44 | **transformism は S2 カットオーバー完遂済み(オーナー裁定 2026-07-15)** → thinkx/kazukiotsukacom 同型で 2026refactor からデプロイ・uwsgi_transformism(8006)起動。CLAUDE.md #4 の「未適用」注記は古い→更新。transformism repo に nginx conf.d が欠けていたので quantz-web 原本から補填。能動ルートは / のみ(他はコメントアウト WIP=意図的凍結) | 実物証拠: 2026refactor HEAD 91ebc16 に vendored libcommon v2.1.0(tree_sha256 = thinkx と同一)・golden・libcommon submodule 除去済み | infra/CLAUDE.md / setup/setup_transformism.sh / transformism repo / nginx-web-root |
| D-43 | staging.<domain> は**実運用の共有 staging URL**として実装する(確認だけの /etc/hosts で済ませない)。各ドメイン: Route53 A(手動・オーナー)+ ワイルドカード証明書 + LB vhost(Host を本番ドメインに書換=web の正しい server ブロックに当てる) | 「実運用のための staging であり実装する。確認だけできればいいのではない」 | Route53(手動)/ loadbalancer repo(2026refactor) |
| D-45 | prod のソース取得は polyrepo 個別 clone でなく **monorepo(thinkx-system, branch monorepo)を 1 回 clone し `/src/<site>` を symlink** で staging と同一レイアウトに再現(`setup/clone_monorepo.sh`)。setup_ 群は clone せず symlink 前提のガードのみ。deploy key は **thinkx-system + libcommon + simplicity の3本**に集約(staging 期の6本から削減)。libcommon/simplicity 原本は ARCHIVE.md の SHA で並置 | I-STEP2 指示「monorepo を clone、libcommon・simplicity の原本を /src に並置」。uwsgi service / nginx include が /src/<site> 前提のため symlink が最小変更 | setup/clone_monorepo.sh / setup/setup_*.sh / deploykeys/gen_deploy_key.sh |
| D-46 | **命名規則を統一(正本 infra/docs/hostname.md)**: 基本名 `{role}{番号}`(web1/lb1)、env は接尾辞 `-stg`(prod 無印)、`supercom-` は共有名前空間(ssh alias・AWS Name タグ)のみ前置。内部 DNS は env なし(VPC 別ゾーンで分離=lb conf が env 非依存)。hostname は env つき(取り違え事故防止)。Host タグ(旧オンプレ名)は廃止。ssh alias も番号つき(「1台のうちは番号なし」案は**一貫性優先で撤回**) | 「web1,web2,web3と増やしていくはずだから最初から番号を」「一貫していないのはバグのもとだからやめよう」 | hostname.md / terraform/instances.tf / setup_webserver.sh / setup_loadbalancer.sh / 構築手順.md |
| D-47 | SSH(22) の許可 IP は**複数リスト**(`my_office_ips`, list(string))。`scripts/add_current_office_ip.sh` は現在地 IP を**追加**(既存を上書きしない)+ tfvars に**追加日付コメント**を記録。**最終アクセス日時**は SG では取れないため 4台の auth.log から実測(`scripts/office_ip_report.sh`、無実績 IP を撤去候補に黄表示)。根治(22 廃止 = SSM 化)は I-STEP2b 改善候補 | 動的 IP 締め出しが 2026-07-18 に実際に発生(拠点移動)。「普段のオフィスのIPが上書きされては困る 追加していけるように」「最終アクセス日時を記録したい セキュリティ上も意味がある」 | terraform/security.tf / variables.tf / scripts/add_current_office_ip.sh / scripts/office_ip_report.sh |
| D-48 | systemd unit を差し替えるスクリプトは `enable --now` でなく **enable + restart**(--now は既起動サービスを再起動せず旧プロセスが残る)。verify は is-active でなく**実プロセスの設定パス(cmdline)と応答ポート**で判定 | prod web で apt 版 nginx が残り 8005/6/7 無応答、verify は偽緑だった(実測) | setup/setup_nginx-web-root.sh |
| D-49 | TLS 自動更新(F14)は**再発行せず** renewal conf の機械置換(authenticator manual→dns-route53)+ 既存 certbot.timer で成立させる。certbot 本体・dns-route53 プラグイン・reload hook・IAM ロール(iam.tf)は構築時に整備済みで、欠落は tarball 持ち込みの conf 1点のみだった。検証は `certbot renew --dry-run`(Route53 実書込を伴う本番同経路)を scripts/check_cert_renewal.sh で随時実行 | 再発行(--force-renewal)は冪等でなくレート制限リスクがあり、conf 置換は冪等で setup_loadbalancer.sh に組める(D-32「もう一度流せば素通り」) | setup/setup_loadbalancer.sh / scripts/check_cert_renewal.sh / iam.tf |
| D-50 | **staging の monorepo 差し替えをカットオーバー前に実施する(オーナー裁定 2026-07-19)**。EC2 は温存し中身(/src)のみ monorepo 化(terraform を伴う本来の I-STEP3 = destroy→再作成はカットオーバー後のまま)。D-32「staging はカットオーバーまで不触」は prod 完成(62/62 green)により参照役目を終えたため解除。これで ROADMAP が本来前提としていた「staging で monorepo 稼働・全ゴールデン green」を DNS 切替前に満たす | 「次はステージングをモノレポに差し替える作業をやる」 | clone_monorepo.sh(旧実ディレクトリ退避)/ 構築手順.md(LB_IP を prerequisites へ) |
| D-51 | **staging は in-place 差し替えでなくゼロから再構築(オーナー裁定 2026-07-19・D-50 の方式を差し替え)** = I-STEP3 の前倒し。理由:「prod と全く同じ状況にするには作り直しが完全な透過性を維持できる」。state は **terraform workspace で分離**(prod=default・staging=staging。同一ローカル state に両環境を持てないため。.tf 変更不要)。順序は **旧 staging destroy(旧 infra リポジトリの state から)→ 新規作成**(EIP 使用が既に10個で上限余裕が不明のため先に解放)。staging.* A レコード5件は新 EIP へ手動付替(構築手順 12) | 「単にステージングのEC2を、VPCをゼロから作り直せば早いのではないだろうか…ゼロから作り直すのが完全な透過性を維持できるのではないだろうか」 | 構築手順.md(workspace 行・手順12)/ 旧 infra リポジトリ(destroy のみ) |
| D-52 | **terraform の環境分離は workspace でなくディレクトリ分離(D-51 の方式を差し替え・オーナー裁定 2026-07-19)**: `infra/terraform/envs/{staging,prod}` が各自の state 台帳と `env.auto.tfvars`(env 指定)を持ち、.tf 本体と terraform.tfvars(my_office_ips)は **symlink で共有**(設定の二重管理なし)。workspace の「いま選ばれている面」という見えない状態を排除し、切替操作そのものを消す。ラッパー(plan-summary / terraform_apply / terraform_destroy / terraform_output)は envs/<env> を対象にする。手順の形は従来どおり「plan-summary <env> → terraform_apply <env>」 | 「ファイルが2つあれば見通しがいいだろう。いちいちテラフォームコマンドで環境を切り替えるというのは、切り替えのミスが生まれるのでなるべくやりたくない」 | infra/terraform/envs/ / scripts/plan-summary.sh / terraform_apply.sh / terraform_destroy.sh / terraform_output.sh / add_current_office_ip.sh |
| D-53 | **EIP は常設台帳 `infra/terraform/eips/`(独立 state)が所有し、環境(envs/)は tag:Name 参照 + association で「借りる」だけ(オーナー裁定 2026-07-19)**。台帳ファイルが「どの EIP がどの環境のどの役割か」の唯一のマスター。環境を destroy しても IP が残るため、staging の作成→破壊サイクルでも DNS 再設定が不要になる。台帳は原則 destroy しない(terraform_destroy.sh は eips を受けない)。未使用期間も EIP は同額課金(約 $3.6/月/本)される点は承知の上 | 「デストロイしても同じEIPを使い回すというやり方もできるだろう」「その台帳の一覧から分かって、それが唯一のマスターファイルになると考えてよいのか。だとしたら、おそらくそれでシンプルにできるから、それで進めていい」 | terraform/eips/main.tf / instances.tf / outputs.tf / scripts/terraform_apply.sh / 構築手順.md 手順1 |
| D-54 | **本番反映は手動ゲート1コマンド(オーナー裁定 2026-07-19)**: `scripts/deploy.sh <env> <サービス...>` = 対象箱で git pull → run/restart_*.sh。prod が変わるのはこの実行の瞬間だけ。**CI は不採用**。将来自動化したくなった場合の正解は **pull 型**(箱側の systemd timer が origin を監視して pull+restart。外から入る経路が不要で SG 設計「22 はオフィス IP のみ」と両立)であり、push 型 CI(GitHub から SSH)は採らない | 「このSSHでログインしてgit pullしてリスタートするという手順が、ほぼローカルからコマンド一発でできるならそれでいいだろう」「(pull 型が正解の件)ディシジョン図に書いておいていいと思う」 | scripts/deploy.sh / docs/サーバー編集のエージェント化計画.md 8章 / 運用.md |
| D-55 | **サーバーからの push を許可(書き込み deploy key・案A 採用/オーナー裁定 2026-07-19)**。乗っ取り時のリポジトリ消失は3層で防ぐ: ①GitHub ブランチ保護(monorepo/master に force push 禁止・削除禁止 = 消失の根絶。オーナーが GitHub 画面で設定)②deploy.sh prod の冒頭で Mac に `git fetch origin`(prod 反映のたびに全履歴のローカルバックアップが自動更新)③別バックアップリポジトリへの push は v2(GitHub アカウント侵害まで守る層)。書き込み鍵 deploy_thinkx-system-rw は **staging web にのみ**置き、push_secrets.sh は `*-rw*` を配布から除外(prod には置かない) | 「基本的にはサーバー上からプッシュできた方が良い。ただし、万が一乗っ取られた場合にレポジトリが消失する危険性を排除するためには…バックアップレポジトリに同じ内容をプッシュしておくのがいい…またはmacに最新のものをプルしてくるか。バックアップする手順があるとしたら、それはプロダクションにデプロイするタイミングが一番いいだろう」 | scripts/deploy.sh / etc/push_secrets.sh / docs/サーバー編集のエージェント化計画.md 5章 |
| D-56 | **GitHub ブランチ保護(force push 禁止)は不採用(D-55 ①を差し替え・オーナー裁定 2026-07-19)**。理由: force push が必要になる場面は必ず出るため運用制約が重い。消失対策は「履歴の完全コピーが常に複数存在する」こと(Mac + 4サーバーの clone + deploy 時の Mac fetch)で担保し、復旧は Mac から push し直す | 「フォースプッシュを禁止することをレポジトリ単位でやると、フォースプッシュが必要になった時に困る…必ずフォースプッシュが必要になる場面というのはそのうち出てくるだろう」 | scripts/deploy.sh(fetch バックアップ)/ DECISIONS D-55 |
| D-57 | **インスタンスサイズを env 独立化・web で prod<staging に反転(オーナー裁定 2026-07-20)**: prod web t3.medium→**t3.small**・prod lb t3.small→**t3.micro**(配信専用で実測 464/265MB と余剰)、staging web t3.small→**t3.medium**(Claude Code+VS Code+ビルドで 2GB では OOM 寸前・swap 0 を実測)。lb は両 env t3.micro。terraform は is_prod 連動をやめ web_type を反転・lb_type 固定。コストは cost-estimate.sh に段階見積もり(稼働率10/30/60/100%)を追加、EIP は D-53 で常時課金のため固定費側に修正。instance_type 変更は stop→modify→start の短時間停止(EIP は台帳で維持)・prod は承認/タイミング要 | 「クオンツを実行しないならスモール/マイクロで足りる、切り替えたい」「staging は実装作業を行うのでスモールで足りるか(メモリ)」「段階見積もりは plan.sh でも出した方がいい」 | terraform/variables.tf / scripts/cost-estimate.sh / plan-summary.sh(cost 呼び出し済) |
